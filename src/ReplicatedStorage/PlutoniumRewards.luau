-- Размеры наград за плутоний (alchemy_td_currencies.md). Начисляет и решает, кому положено,
-- Plutonium (ServerScriptService) — здесь только числа.
--
-- Магазин, который тратит плутоний, — отдельное ТЗ и пока не существует, поэтому абсолютные
-- значения ни на что не откалиброваны: важны пропорции (награда за уровень заметно
-- крупнее награды за волну), не сами числа.
local PlutoniumRewards = {
	waveClear = 5, -- первое прохождение волны (= новый рекорд на уровне, см. Plutonium.AwardWaveClear)
	levelClear = 50, -- первое прохождение уровня целиком, разово, поверх наград за волны
	enemyFirstKill = 3, -- первое убийство нового типа жука, по одному разу за тип
}

return PlutoniumRewards
